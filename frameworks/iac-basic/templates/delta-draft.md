---
human_revised: false
plan: <PLAN-ID>
status: draft
date: YYYY-MM-DD
---

# Delta draft — <PLAN-ID>

Proposed changes to `topology/` resulting from this plan. **The Lead validates and finalizes** as `archive/<PLAN-ID>/delta.md` during the archive flow. Do not edit `topology/` directly.

If no spec change is needed (e.g. the plan was a tooling fix that does not alter system behavior), replace the body below with a single line:

> No spec change required — &lt;one-line rationale&gt;.

---

## topology/&lt;area&gt;/&lt;concern&gt;.md

### Added Requirements

- WHEN &lt;trigger&gt; THE SYSTEM SHALL &lt;new response&gt;.

### Modified Requirements

- WHEN &lt;trigger&gt; THE SYSTEM SHALL &lt;new response&gt;
  (was: WHEN &lt;trigger&gt; THE SYSTEM SHALL &lt;old response&gt;)

### Removed Requirements

- (Quoted previous requirement, with reason for removal.)

## topology/&lt;another-area&gt;/index.md

### Added Requirements

- ...
