---
human_revised: false
version: <TO-ref>
compared-from: <FROM-ref>
generated-at: <YYYY-MM-DD>
---

# Release <TO-ref>

> Narrated from the GitLab compare `<FROM-ref>..<TO-ref>`. Keep these sections, in this order,
> so every release report reads the same. Narrate the raw range — don't paste commit titles.

## Overview

One or two paragraphs: the theme of this release — what it's mostly about and who it's for.
Grounded in the actual changes, not marketing.

## Highlights

- The handful of changes that matter most, in user-facing terms.

## Changes by area

Group changes by component/area (use the project's `meta.apps.values` when they map). A short
bullet per meaningful change under each; fold trivial churn (typos, formatting) into one line.

## Fixes

- Bug fixes worth calling out.

## Breaking changes

Behaviour/API breaks and the migration each requires. Write `None` explicitly if there are none.

## Contributors

The authors in the range.

## Range

- Compared: `<FROM-ref>` → `<TO-ref>`
- Source: GitLab project `<project>`
- Commits: `<N>` · Files changed: `<N>`
